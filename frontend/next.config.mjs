/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['localhost', 'images.pexels.com', 'www.crackadmission.com'], // Add multiple domains in a single array
  },
};

export default nextConfig;
